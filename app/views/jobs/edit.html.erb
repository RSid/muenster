<h1>Edit Job</h1>

<%= form_for @job do |f| %>
  <%= f.label :title %>:
  <%= f.text_field :title %>

  <%= f.label :description %>:
  <%= f.text_field :description %>

  <%= f.label :company %>:
  <%= f.collection_select(:company_id, Company.all, :id, :name) %>

  <%= f.submit %>

<% end %>
