<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Jobs</title>
</head>
<body>

<ul>
  <% @jobs.each do |job| %>
  <li><%= "#{job.title} - #{job.company.name}, #{job.company.location}" %></li>
  <%= link_to(job.title, job_path(job)) %>
  <% end %>
</ul>

</body>
</html>
